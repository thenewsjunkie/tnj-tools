

## Rename "Subscription Gifters" to "Secret Shows Gift Leaders"

Simple text rename across two files.

### Changes

**1. `src/components/studio/SecretShowsLeaderboard.tsx`** (line 48)
- Change `"Subscription Gifters"` to `"Secret Shows Gift Leaders"` in the CardTitle

**2. `src/pages/SecretShowsLeaderboard.tsx`** (line 23)
- Change `"Subscription Gifter Leaderboard"` to `"Secret Shows Gift Leaders"` in the heading

**3. `src/pages/SecretShowsLeaderboard.tsx`** (line 25)
- Update the subtitle from `"Top supporters who gift Secret Shows subscriptions"` to something like `"Top supporters gifting Secret Shows subscriptions"` (minor tweak for consistency, optional)

