

## Reduce Rundown Title Size

Drop the page title one more step from `text-6xl` (60px) to `text-5xl` (48px).

### File to modify

**`src/pages/RundownPage.tsx`** -- Change the `<h1>` class from `text-6xl` to `text-5xl`. Single class swap, nothing else changes.

